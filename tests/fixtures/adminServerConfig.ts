import type { AdminServerConfig } from "@/api/adminServerConfig";

const BASE: AdminServerConfig = {
  serverName: "Test Server",
  serverDescription: "A test server",
  accentColor: null,
  registrationEnabled: true,
  termsContent: "",
  privacyContent: "",
  legalContactEmail: null,
  requireRegistrationConsent: false,
  listInServerCatalogue: false,
  minimumAgeYears: 13,
  messageRetentionDays: 0,
  notificationRetentionDays: 90,
  archivedChannelRetentionDays: 0,
  defaultLocale: "en",
  defaultAttachmentRetentionDays: null,
  maxAttachmentSizeMb: 25,
  maxAttachmentsPerMessage: 5,
  defaultWelcomeChannelName: "welcome",
  rateLoginLimit: 5,
  rateLoginIntervalSeconds: 900,
  rateSessionRefreshLimit: 60,
  rateSessionRefreshIntervalSeconds: 60,
  rateRegisterLimit: 5,
  rateRegisterIntervalSeconds: 3600,
  rateApiWriteLimit: 120,
  rateApiWriteIntervalSeconds: 60,
  rateMessageSendLimit: 30,
  rateMessageSendIntervalSeconds: 60,
  rateAttachmentUploadLimit: 20,
  rateAttachmentUploadIntervalSeconds: 60,
  rateSearchLimit: 30,
  rateSearchIntervalSeconds: 60,
  smtpHost: null,
  smtpPort: null,
  smtpUsername: null,
  smtpPasswordSet: false,
  smtpEncryption: null,
  smtpFromEmail: null,
  smtpFromName: null,
  avatarMaxSizeMb: 5,
  avatarMaxWidth: 512,
  avatarMaxHeight: 512,
  logoMaxSizeMb: 5,
  logoMaxWidth: 1024,
  logoMaxHeight: 1024,
  communityEmojiMaxSizeMb: 2,
  attachmentAllowedMimes: "image/png,image/jpeg",
  communityEmojiAllowedMimes: "image/png,image/gif",
  diskPurgeTriggerPercent: 0,
  diskPurgeTargetPercent: 15,
  diskPurgeMinAgeDays: 14,
  diskPurgeIncludeDms: false,
  autoTimeoutEnabled: false,
  autoTimeoutHits: 5,
  autoTimeoutWindowSeconds: 300,
  autoTimeoutDurationSeconds: 60,
  autoTimeoutProgressive: false,
  autoTimeoutResetSeconds: 3600,
  autoTimeoutMaxSeconds: 86400,
  ipReputationEnabled: false,
  ipReputationEndpoint: "",
  ipReputationConfidenceMin: 90,
  ipReputationCheckUsername: false,
  ipReputationAllowlist: "",
  ipReputationAppealContact: "",
  resetPasswordCodeExpiryMinutes: 30,
  emailChallengeExpiryMinutes: 60,
  invitationExpiryHours: 72,
  validateEmails: false,
  digestHour: 8,
  mediaTokenTtlSeconds: 3600,
  communityEmojiTokenTtlSeconds: 86400,
  defaultBotId: 0,
  welcomeBotId: 0,
  autoModeratorBotId: 0,
  updatedAt: "2026-01-01T00:00:00Z",
  updatedBy: null,
};

export function makeAdminServerConfig(
  overrides: Partial<AdminServerConfig> = {},
): AdminServerConfig {
  return { ...BASE, ...overrides };
}
